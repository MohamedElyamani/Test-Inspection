/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EquipmentmoreinformationService } from './equipmentmoreinformation.service';

describe('Service: Equipmentmoreinformation', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EquipmentmoreinformationService]
    });
  });

  it('should ...', inject([EquipmentmoreinformationService], (service: EquipmentmoreinformationService) => {
    expect(service).toBeTruthy();
  }));
});
